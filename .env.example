# Task Master Environment Variables
# Copy this file to .env in your project root or globally in your home directory

# =============================================================================
# API KEYS (Required for corresponding providers)
# =============================================================================

# Anthropic (Claude models)
ANTHROPIC_API_KEY="your_anthropic_api_key_here"       # Required: Format: sk-ant-api03-...

# Perplexity (Research models)
PERPLEXITY_API_KEY="your_perplexity_api_key_here"     # Optional: Format: pplx-...

# OpenAI (GPT models)
OPENAI_API_KEY="your_openai_api_key_here"             # Optional, for OpenAI/OpenRouter models. Format: sk-proj-...

# Google (Gemini models)
GOOGLE_API_KEY="your_google_api_key_here"             # Optional, for Google Gemini models.

# Mistral AI
MISTRAL_API_KEY="your_mistral_key_here"               # Optional, for Mistral AI models.

# Azure OpenAI (requires both key and endpoint)
AZURE_OPENAI_API_KEY="your_azure_key_here"            # Optional, for Azure OpenAI models (requires endpoint in .taskmaster/config.json).
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/

# OpenRouter
OPENROUTER_API_KEY="your_openrouter_api_key_here"

# xAI (Grok models)
XAI_API_KEY="YOUR_XAI_KEY_HERE"                       # Optional, for xAI AI models.

# Ollama (local models)
OLLAMA_API_KEY="your_ollama_api_key_here"             # Optional: For remote Ollama servers that require authentication.
OLLAMA_BASE_URL=http://localhost:11434/api

# =============================================================================
# GLOBAL MODEL DEFAULTS (New Feature!)
# Set these to avoid configuring models for every new project
# =============================================================================

# Main Model (for primary task generation and updates)
TASKMASTER_DEFAULT_MAIN_PROVIDER="claude-code"
TASKMASTER_DEFAULT_MAIN_MODEL="default"
TASKMASTER_DEFAULT_MAIN_MAX_TOKENS=120000
TASKMASTER_DEFAULT_MAIN_TEMPERATURE=0.2

# Research Model (for research-backed operations with --research flag)
TASKMASTER_DEFAULT_RESEARCH_PROVIDER="claude-code"
TASKMASTER_DEFAULT_RESEARCH_MODEL="default"
TASKMASTER_DEFAULT_RESEARCH_MAX_TOKENS=120000
TASKMASTER_DEFAULT_RESEARCH_TEMPERATURE=0.1

# Fallback Model (used when main model fails)
TASKMASTER_DEFAULT_FALLBACK_PROVIDER="claude-code"
TASKMASTER_DEFAULT_FALLBACK_MODEL="default"
TASKMASTER_DEFAULT_FALLBACK_MAX_TOKENS=120000
TASKMASTER_DEFAULT_FALLBACK_TEMPERATURE=0.2

# =============================================================================
# GLOBAL SETTINGS DEFAULTS
# =============================================================================

# Logging and Debug
TASKMASTER_DEFAULT_LOG_LEVEL="info"
TASKMASTER_DEFAULT_DEBUG="false"

# Task Generation Defaults
TASKMASTER_DEFAULT_SUBTASKS=5
TASKMASTER_DEFAULT_PRIORITY="medium"
TASKMASTER_DEFAULT_PROJECT_NAME="Task Master"

# Service URLs
TASKMASTER_DEFAULT_BEDROCK_URL="https://bedrock.us-east-1.amazonaws.com"

# =============================================================================
# EXAMPLE: Alternative Configuration for Pay-per-Token Models
# =============================================================================

# If you prefer to use pay-per-token models instead of Claude Code CLI:
# TASKMASTER_DEFAULT_MAIN_PROVIDER=anthropic
# TASKMASTER_DEFAULT_MAIN_MODEL=claude-sonnet-4-20250514
# TASKMASTER_DEFAULT_RESEARCH_PROVIDER=perplexity
# TASKMASTER_DEFAULT_RESEARCH_MODEL=sonar-pro
# TASKMASTER_DEFAULT_FALLBACK_PROVIDER=anthropic
# TASKMASTER_DEFAULT_FALLBACK_MODEL=claude-3-5-sonnet-20241022

# =============================================================================
# USAGE NOTES
# =============================================================================

# 1. Global Setup: Place this .env file in your home directory to set defaults for all projects
# 2. Project Override: Place a .env file in individual project roots to override global defaults
# 3. Claude Code CLI: Requires 'npm install -g @anthropic-ai/claude-code' and active subscription
# 4. Zero Cost: Claude Code CLI uses flat subscription pricing (no per-token charges)
# 5. All Roles: With the above config, Claude Code CLI will be used for main, research, and fallback operations